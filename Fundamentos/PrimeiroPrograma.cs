namespace CursoCSharp.Fundamentos {
    internal class PrimeiroPrograma {
        public static void Executar() {
            Console.Write("First "); // NEXT STRING Stays in the same line (Write).
            Console.WriteLine("Program"); // Goes to the next line/Breaks the line (Write Line).
            Console.WriteLine("The End!");
        }
    }
}
